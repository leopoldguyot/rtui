# Create a mutable reactive value

Creates a reactive value container. Call without arguments to read, and
with one argument to update.

## Usage

``` r
tuiReactiveVal(value = NULL)
```

## Arguments

- value:

  Initial value.

## Value

A reactive value function.
