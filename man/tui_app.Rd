% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{tui_app}
\alias{tui_app}
\title{Create a TUI application}
\usage{
tui_app(state = list(), ui, handlers = list())
}
\arguments{
\item{state}{A named list representing the initial application state.}

\item{ui}{A UI component tree built with \code{\link[=tui_vbox]{tui_vbox()}}, \code{\link[=tui_hbox]{tui_hbox()}},
\code{\link[=tui_render_text]{tui_render_text()}}, \code{\link[=tui_input_button]{tui_input_button()}}, or \code{\link[=tui_input_text]{tui_input_text()}}.}

\item{handlers}{A named list of functions. Each function receives the
current state list and must return the updated state list.}
}
\value{
An object of class \code{rtui_app}.
}
\description{
Defines a terminal UI application with a reactive state, a UI component
tree, and named event handlers.
}
\examples{
app <- tui_app(
  state = list(counter = 0L),
  ui = tui_vbox(
    tui_render_text("counter"),
    tui_input_button("Increment", id = "inc")
  ),
  handlers = list(
    inc = function(state) {
      state$counter <- state$counter + 1L
      state
    }
  )
)

}
