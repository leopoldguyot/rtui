% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{tuiObserveEvent}
\alias{tuiObserveEvent}
\title{Run code only for selected input events}
\usage{
tuiObserveEvent(event, expr, runAtInit = FALSE)
}
\arguments{
\item{event}{An \verb{input$<id>} or \code{input[["id"]]} reference.}

\item{expr}{An expression evaluated only when \code{event} matches the triggering
input id.}

\item{runAtInit}{A single logical value. If \code{TRUE}, run \code{expr} once during
app initialization before any input event.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Evaluates \code{expr} only when the current server execution was triggered by one
of the given input ids. This is useful to isolate updates so they run only
after explicit actions (for example an "Apply" button).
}
\examples{
app <- tuiApp(
  ui = tuiColumn(
    tuiRenderText(output$name),
    tuiInputButton("Apply name", id = "applyName"),
    tuiInputText("nameInput", value = "John")
  ),
  server = function(input, output) {
    tuiObserveEvent(input$applyName, runAtInit = TRUE, {
      output$name <- input$nameInput
    })
  }
)

}
