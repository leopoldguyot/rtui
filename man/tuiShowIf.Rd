% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{tuiShowIf}
\alias{tuiShowIf}
\title{Conditionally render a child component by terminal size}
\usage{
tuiShowIf(
  child,
  minTerminalWidth = NULL,
  maxTerminalWidth = NULL,
  minTerminalHeight = NULL,
  maxTerminalHeight = NULL
)
}
\arguments{
\item{child}{A single \code{rtuiComponent} to conditionally render.}

\item{minTerminalWidth, maxTerminalWidth}{Optional minimum/maximum terminal
width (in terminal cells) required to show \code{child}.}

\item{minTerminalHeight, maxTerminalHeight}{Optional minimum/maximum terminal
height (in terminal cells) required to show \code{child}.}
}
\value{
A \code{rtuiComponent} list node of type \code{"showIf"}.
}
\description{
Wraps a single child component and shows it only when terminal dimensions
satisfy the specified constraints. This is useful for responsive breakpoints
(for example, hiding secondary panels on narrow terminals) while keeping a
declarative static UI tree.
}
\details{
Conditions are evaluated against the \strong{current terminal size} and rechecked
during rendering, so visibility updates automatically when the terminal is
resized.
}
